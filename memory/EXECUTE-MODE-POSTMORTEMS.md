# Execute Mode — Postmortems (Global)

Purpose: when Luke has to tell Mac “just do it” / “execute mode”, we log a tight postmortem so the behavior improves permanently.

Format: **exactly 3 bullets**
- **Hesitation cause:** …
- **Guardrail misapplied:** …
- **Fix:** …

---

## 2026-02-14 — (seed)
- **Hesitation cause:** I defaulted to option-presenting (“what we can do next…”) instead of executing obvious internal work.
- **Guardrail misapplied:** I treated “Luke approval required” as applying to all internal drafting/process updates, not just external ship + risky operations.
- **Fix:** Locked global execute-mode default + added this enforcement postmortem log.
