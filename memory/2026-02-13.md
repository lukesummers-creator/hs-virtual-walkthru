# 2026-02-13 — Checkpoint

## Where we left off
- Luke asked for **grocery substitution rules**. Found canon doc at `projects/groceries/GROCERIES-RULES.md` and answered from it.
- Luke asked if the **continuity-recall hook** was working.
  - Confirmed the hook fires on `/new` (we saw the nudge message).
  - Likely reason session-start injection isn’t visible: `hooks/continuity-recall/handler.js` bails out when it can’t extract an initial prompt (`if (!prompt) return;`). On a fresh session there’s often no prompt yet.
  - Proposed fix: always inject baseline context (core rules / vault anchors) even with empty prompt; only do recall-index matching when prompt exists.
- Luke asked about “MacVault magic” and how far off we are.
  - Summary: baseline injection fix will improve *feel* immediately; real reliability also depends on having sharp canon notes.
- Luke asked where **proposal red rules** live.
  - Located canon docs: `projects/homestretch-proposals/PROCESS.md`, `projects/homestretch-proposals/PRICING-JOURNAL-RULES.md`, templates, plus global `projects/GUARDRAILS.md`.
  - Drafted new scannable index: `projects/homestretch-proposals/RED-RULES.md` (LOCKED/SOFT/TBD, with sources).
- Luke challenged Mac: repeated failures to **backcheck active work after “lock that in”** in the Proposals Telegram channel. Mac acknowledged this is a process failure and committed to treating “lock that in” as a 3-step transaction: Promote → Backcheck → Proof (no “locked” without proof).

## New/changed artifacts
- New file: `projects/homestretch-proposals/RED-RULES.md`

## Explicit asks from Luke (pending)
- Luke will review `RED-RULES.md` and mark items: LOCK / SOFT / WRONG / ADD.
- Luke will send at least one example from Telegram showing “lock that in” + later reminder to backcheck.

## Next actions (when resuming)
1) Patch hook: modify session:start logic to inject baseline context even without prompt.
2) Wire proposal RED-RULES into the rest of canon (index links in PROCESS/templates) once Luke approves (“ship it”).
3) Implement/encode the “lock that in” backcheck/proof discipline as the default operating behavior.
