# 2026-02-14 — Notes

## Nightly backup flows
- 02:15 CT reminder: ran sync + commit + push for `/Users/lrs/mac-brain`.
  - Synced clawd memory into `mac-brain/memory/`.
  - Committed + pushed `Nightly checkpoint` (commit `91e3640` on `master`).
- 02:20 CT automatic flow: ran full nightly backup.
  - Sync: done.
  - `mac-brain` status: clean (no new commit).
  - Zip snapshots to Google Drive: success.
    - Output folder: `.../mac/openclaw-nightly-zips/2026-02-14_022012/`
    - Created: `workbench_clawd_...022012.zip` and `brain_mac-brain_...022012.zip`.

## Continuity / process reminders
- Proposals: Luke flagged repeated misses where Mac said “lock that in” but failed to backcheck active artifacts. Future behavior must be: Promote → Backcheck → Proof (no “locked” without proof).

## Tooling constraints noticed
- `memory_search` tool currently disabled due to missing API keys (OAuth auth present but provider config missing). Rely on local repo search + docs until fixed.
