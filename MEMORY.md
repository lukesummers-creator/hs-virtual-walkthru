# MEMORY.md (Long-term)

This file is the curated, durable memory for the workspace.

## Sacred continuity rule
Everything that drives the brain (continuity, process, SOPs, templates, lookup tables, indices, tools) must be stored and backed up in the private repo:
- https://github.com/lukesummers-creator/clawd-notes

**Important:** there is also a separate “brain repo” on disk at `/Users/lrs/mac-brain` (GitHub: `lukesummers-creator/mac-brain`). It contains continuity/OS design docs (e.g., `continuity/OS-WORKSHOP-PLAN.md`). When Luke says “in the brain,” check **mac-brain** first.

Exclude large/binary artifacts (PDF/XLSX/zips/photos) and secrets; instead store links + rehydration steps.

## Resume strategy when memory is empty
When “memory” is incomplete or uncertain:
- verify via project artifacts and canonical docs (PROCESS/SOPs/templates/pricing tables) rather than relying on recall.
