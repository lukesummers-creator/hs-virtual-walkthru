State: Active

NEXT: Define guardrail policy + implement as lightweight checks (docs + process + config where needed).

BLOCKED BY: None.

Deploy target: `projects/*` docs + (maybe) gateway/channel config (only if Luke approves).
